async function refresh(){const res=await fetch('/api/bundles');const data=await res.json();const bundleList=document.getElementById('bundle-list');bundleList.innerHTML='';data.bundles.forEach(b=>{const div=document.createElement('div');div.className='card';div.innerHTML=`<strong>${b.name}</strong><br><small>${b.location}</small><br>`;const btn=document.createElement('button');btn.textContent='Install';btn.onclick=async()=>{const r=await fetch('/api/install',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({bundle_filename:b.name})});const result=await r.json();alert(result.message||JSON.stringify(result));refresh();};div.appendChild(btn);bundleList.appendChild(div);});const installed=document.getElementById('installed-list');installed.innerHTML='';data.installed.forEach(a=>{const div=document.createElement('div');div.className='card';div.innerHTML=`<strong>${a.name}</strong> (${a.version})<br><small>Port: ${a.port||''}</small>`;const btn=document.createElement('button');btn.textContent='Remove';btn.onclick=async()=>{const r=await fetch('/api/remove',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({app_id:a.id})});const result=await r.json();alert(result.message||JSON.stringify(result));refresh();};div.appendChild(btn);installed.appendChild(div);});}
document.getElementById('upload-form').addEventListener('submit', async e=>{e.preventDefault();const fd=new FormData();fd.append('file',document.getElementById('bundle-file').files[0]);const res=await fetch('/api/upload',{method:'POST',body:fd});document.getElementById('upload-result').textContent=JSON.stringify(await res.json(),null,2);refresh();});refresh();