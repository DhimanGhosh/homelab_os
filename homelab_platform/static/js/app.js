function setOutput(text){document.getElementById('operation-output').textContent=text;}
function setLog(text){document.getElementById('log-output').textContent=text;}
async function viewLog(appId){const res=await fetch(`/api/logs/${appId}`);const data=await res.json();setLog(data.content||data.message||JSON.stringify(data,null,2));}
async function refresh(){const res=await fetch('/api/bundles');const data=await res.json();const bundleList=document.getElementById('bundle-list');bundleList.innerHTML='';const installedMap=new Map((data.installed||[]).map(x=>[x.id,x]));data.bundles.forEach(b=>{const appId=b.name.split('.app.')[0].replace(/_/g,'-');const installed=installedMap.get(appId);const div=document.createElement('div');div.className='card';const status=installed?`<span class="badge ${installed.install_status||'unknown'}">${installed.install_status||'unknown'}</span>`:'';div.innerHTML=`<strong>${b.name}</strong> ${status}<br><small>${b.location}</small><br>`;const btn=document.createElement('button');btn.textContent=installed && installed.install_status==='installed'?'Reinstall':'Install';btn.onclick=async()=>{setOutput(`Installing ${b.name}...`);const r=await fetch('/api/install',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({bundle_filename:b.name})});const result=await r.json();setOutput(result.message||JSON.stringify(result,null,2));refresh();};div.appendChild(btn);if(installed?.log_path){const logBtn=document.createElement('button');logBtn.textContent='View Log';logBtn.onclick=()=>viewLog(installed.id);div.appendChild(logBtn);}bundleList.appendChild(div);});const installed=document.getElementById('installed-list');installed.innerHTML='';(data.installed||[]).forEach(a=>{const div=document.createElement('div');div.className='card';div.innerHTML=`<strong>${a.name}</strong> (${a.version||''}) <span class="badge ${a.install_status||'unknown'}">${a.install_status||'unknown'}</span><br><small>Port: ${a.port||''}</small>${a.last_error?`<br><small class="error">${a.last_error}</small>`:''}`;const btn=document.createElement('button');btn.textContent='Remove';btn.onclick=async()=>{setOutput(`Removing ${a.id}...`);const r=await fetch('/api/remove',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({app_id:a.id})});const result=await r.json();setOutput(result.message||JSON.stringify(result,null,2));refresh();};div.appendChild(btn);if(a.log_path){const logBtn=document.createElement('button');logBtn.textContent='View Log';logBtn.onclick=()=>viewLog(a.id);div.appendChild(logBtn);}installed.appendChild(div);});}
document.getElementById('upload-form').addEventListener('submit', async e=>{e.preventDefault();const fd=new FormData();fd.append('file',document.getElementById('bundle-file').files[0]);const res=await fetch('/api/upload',{method:'POST',body:fd});const result=await res.json();document.getElementById('upload-result').textContent=JSON.stringify(result,null,2);refresh();});refresh();