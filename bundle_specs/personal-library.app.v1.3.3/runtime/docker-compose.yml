services:
  personal-library:
    build: .
    container_name: personal-library
    restart: unless-stopped
    ports:
      - "127.0.0.1:8132:8132"
    volumes:
      - ./data:/opt/personal-library/data
