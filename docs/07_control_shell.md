
# Global Shell

The global shell is represented by the shared assets under `core/templates/` and `core/static/`.

Design targets:
- shared header
- plugin content mount area
- notifications drawer
- quick actions
- mobile navigation

The current shell files are minimal scaffolding intended for progressive enhancement.
