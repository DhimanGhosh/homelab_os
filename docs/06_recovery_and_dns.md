# Recovery and DNS

The repository includes `recovery/self_recovery_v3_4/`, which is the working recovery baseline that restored Docker Root Dir on HDD, cloudflared pinned to `cloudflare/cloudflared:2026.1.2`, Pi-hole health on `127.0.0.1:8080`, Caddy HTTPS routing, and Tailscale certificate generation.
