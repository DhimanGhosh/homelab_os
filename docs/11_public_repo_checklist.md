# Public Repo Checklist

- Remove secrets from `.env`
- Keep `.env.example` only
- Exclude `.venv/`, runtime output, and machine logs
- Publish generated `.tgz` bundles via releases, not source branch blobs
